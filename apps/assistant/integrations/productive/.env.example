# Copy to .env (gitignored) OR set these in your Claude environment settings.
# Productive: Settings → API integrations → Generate new token (read-only if offered).
PRODUCTIVE_API_TOKEN=
PRODUCTIVE_ORG_ID=
